# Express.js Query Parameters

This folder contains Express.js query parameter examples and practice exercises.

Query parameters allow additional information to be passed through the URL and are commonly used for searching, filtering, and sorting data.

---

## Purpose

- Understand query parameters
- Learn how to use `req.query`
- Practice single and multiple query parameters
- Understand the difference between route and query parameters
- Learn common uses of query parameters